{
  "name": "shin-shop-backend-deploy",
  "version": "1.0.0",
  "description": "Deployment package for Shin Shop Medusa.js backend",
  "scripts": {
    "build": "cd medusa-backend && npm install && npm run build:prod",
    "start": "cd medusa-backend && npm run start",
    "postinstall": "cd medusa-backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
